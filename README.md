# ISO20022-Validator-Ruleset-Studio
A ruleset-driven ISO 20022 XML validator that auto-detects message types, applies layered core and enterprise rules (e.g. SWIFT gpi), and generates audit-ready Excel and JSON reports. Supports single and batch validation and Docker-based deployment.
